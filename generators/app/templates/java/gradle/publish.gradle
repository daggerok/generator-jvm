allprojects {
  apply plugin: 'maven-publish'

  publish {
    publications {
      shadow(MavenPublication) { publication ->
        project.shadow.component(publication)
      }
    }
    repositories {
      maven {
        url "$rootDir.buildDir/maven-publish"
      }
    }
  }
}
